{
  "name": "loan-saas-monorepo",
  "version": "1.3.0",
  "description": "Monorepo for B2B Loan Management and SaaS Subscription platform",

  "private": true,
  "scripts": {
    "install:all": "npm install && npm install --prefix backend && npm install --prefix frontend",
    "dev": "concurrently \"npm run dev --prefix backend\" \"npm run start --prefix frontend\"",
    "build:all": "npm run build --prefix backend && npm run build --prefix frontend"
  },
  "dependencies": {
    "concurrently": "^8.2.2"
  }
}
