# Connection URL to PostgreSQL (Neon DB recommended)
DATABASE_URL="postgresql://user:password@host/dbname?sslmode=require"

# Secret key for JWT signing/verification
JWT_SECRET="caterpillar-industrial-super-secret-key"

# Port configuration
PORT=3000
